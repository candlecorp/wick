---
title: HTTP Server
sidebar_position: 1
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# nanobus.transport.http.server/v1

<div class="attributes">

### Description

<p>
  <span className="badge badgeDarkBlue">API</span>
  <a href="https://github.com/nanobus/nanobus/blob/main/pkg/transport/http/server.go" target="_blank" rel="noopener noreferrer">Source code <svg width="13.5" height="13.5" aria-hidden="true" viewBox="0 0 24 24" class="iconExternalLink_node_modules-@docusaurus-theme-classic-lib-theme-Icon-ExternalLink-styles-module"><path fill="currentColor" d="M21 13v10h-21v-19h12v2h-10v15h17v-8h2zm3-12h-10.988l4.035 4-6.977 7.07 2.828 2.828 6.977-7.07 4.125 4.172v-11z"></path></svg></a>
</p>

</div>

This component provides extensible HTTP server capabilities.
The functionality of your application is added
by including `routers` and `middleware` components.

## Options

<div class="attributes">

### address

<div class="attribute">
  <header>
    <span className="badge badgeDarkBlue">address <span className="required">*</span>
      <a class="hash-link" href="#address" title="Direct link to address">​</a>
    </span>
    <code>string</code>
  </header>

The listening address of the server.

</div>

### middleware

<div class="attribute">
  <header>
    <span className="badge badgeDarkBlue">middleware
      <a class="hash-link" href="#middleware" title="Direct link to middleware">​</a>
    </span>
    <code>Component[]</code>
  </header>

Array of [HTTP Middleware](/category/http-middleware) component configurations.

</div>

### routers

<div class="attribute">
  <header>
    <span className="badge badgeDarkBlue">routers
      <a class="hash-link" href="#routers" title="Direct link to routers">​</a>
    </span>
    <code>Component[]</code>
  </header>

Array of [HTTP Router](/category/http-routers) component configurations.

</div>

</div>

## Examples

### Basic configuration

<Tabs
  values={[
    {label: 'YAML', value: 'yaml'},
    {label: 'TypeScript', value: 'typescript'},
  ]}>
  <TabItem value="yaml">

```yaml
transports:
  http:
    uses: "nanobus.transport.http.server/v1"
    with:
      address: ':8080'
      middleware:
        ### See middleware
      routers:
        ### See routers
```

  </TabItem>
  <TabItem value="typescript">

```typescript
import { HttpServerV1 } from "https://deno.land/x/nanobusconfig@v0.0.14/mod.ts";

app.transport(
  "http",
  HttpServerV1({
    address: ":8080",
    // See middleware
    middleware: [],
    // See routers
    routers: [],
  }),
);
```

  </TabItem>
</Tabs>
