---
version: 0
host:
  allow_latest: true
network:
  collections:
    - namespace: markdown
      kind: WaPC
      reference: REGISTRY_TODO.azurecr.io/vino/markdown:latest
  schematics:
    - name: render
      collections:
        - markdown
      instances:
        render: markdown::render
      connections:
        - <> => render[markdown]
        - render[html] => <>
