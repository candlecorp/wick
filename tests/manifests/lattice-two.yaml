version: 0
host:
  rpc:
    enabled: true
  lattice:
    enabled: true
network:
  name: network-two
  schematics:
    - name: logger
      instances:
        log:
          id: vino::core::log
      connections:
        - <>[child_input] => log[input]
        - log[output] => <>[child_output]
