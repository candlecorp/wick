---
version: 0
network:
  schematics:
    - name: simple
      instances:
        add:
          id: vino::v0::add
        add2:
          id: vino::v0::add
      connections:
        - <> => add[left]
        - <> => add[right]
        - <> => add2[right]
        - add[output] => add2[left]
        - add2[output] => <>
