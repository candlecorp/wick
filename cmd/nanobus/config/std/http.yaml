version: 0
id: test
interfaces:
  test:
    test:
      steps:
        - uses: http
          with:
            url: http://httpbin.org/status/200
            method: GET
    decode:
      steps:
        - uses: http
          with:
            url: http://httpbin.org/get?value=test-value
            method: GET
          returns: 'response'
        - uses: expr
          with:
            value: 'response.args'
