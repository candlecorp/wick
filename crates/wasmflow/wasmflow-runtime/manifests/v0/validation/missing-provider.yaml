---
version: 0
network:
  providers:
    - namespace: wapc
      kind: WaPC
      reference: nothing.wasm
  schematics:
    - name: missing_provider
      instances:
        validate:
          id: wapc::validate
      connections:
        - <> => validate[input]
        - validate[output] => <>
