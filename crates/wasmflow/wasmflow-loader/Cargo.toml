[package]
name = "wasmflow-loader"
version = "0.0.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tracing = "0.1"
thiserror = "1.0"
wasmflow-oci = { path = "../../wasmflow/wasmflow-oci" }
tokio = { version = "1", features = ["fs"] }
