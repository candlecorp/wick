---
version: "0"
default_schematic: "logger"
network:
  schematics:
    - name: logger
      instances:
        logger: vino::log
      connections:
        - <> => logger[input]
        - logger[output] => <>
