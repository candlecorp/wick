---
version: "0"
default_schematic: "logger"
network:
  schematics:
    - name: logger
      components:
        logger:
          id: vino::log
      connections:
        - <> => logger[input]
        - logger[output] => <>
