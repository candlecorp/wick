"v1.{{name.value}}": {
  "oneOf" : [
    {{#withAnnotation "shortform" ~}}
    { "type":"string" },
    {{~/withAnnotation}}
    {{#unlessAnnotation "shortformonly"}}
    {{#join types "," ~}}
    {"$ref": "#/$defs/v1.{{name.value}}"}
    {{~/join}}
    {{/unlessAnnotation}}
  ]
},
