namespace "set-scan"

type Inputs {
  "The set's key."
  key: string
  "The cursor position."
  cursor: string
  "The number of elements to return."
  count: u32
}

type Outputs {
  "The values in the set."
  values: [string]
  "The cursor for future scans."
  cursor: string
}
