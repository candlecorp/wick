name: grpc
providers:
  - namespace: test-namespace
    kind: GrpcUrl
    reference: https://127.0.0.1:$TEST_PORT
components:
  external:
    id: test-namespace::test-component
connections:
  - from:
      reference: <input>
      port: input
    to:
      reference: external
      port: input
  - from:
      reference: external
      port: output
    to:
      reference: <output>
      port: output
