---
schematics:
  - name: wapc_component
    providers:
      - namespace: wapc
        kind: WaPC
        reference: ../integration/test-wapc-component/build/test_component_s.wasm
    instances:
      validate:
        id: wapc::validate
      log:
        id: vino::v0::log
    connections:
      - from:
          instance: <input>
          port: input
        to:
          instance: log
          port: input
      - from:
          instance: log
          port: output
        to:
          instance: validate
          port: input
      - from:
          instance: validate
          port: output
        to:
          instance: <output>
          port: output
