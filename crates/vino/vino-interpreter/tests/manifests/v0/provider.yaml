---
version: 0
network:
  schematics:
    - name: main
      instances:
        INSTANCE:
          id: test::instance
      connections:
        - <> => INSTANCE[input]
        - INSTANCE[output] => <>
