---
version: 0
network:
  providers:
    - namespace: wapc
      kind: WaPC
      reference: ../../integration/test-wapc-component/build/test_component_s.wasm
  schematics:
    - name: test
      providers:
        - wapc
      instances:
        copy:
          id: wapc::copy
      connections:
        - <> => copy[input]
        - "5 => copy[times]"
        - copy[output] => <>
