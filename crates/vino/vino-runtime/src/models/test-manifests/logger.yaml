version: 0
network:
  providers:
    - namespace: wapc
      kind: WaPC
      reference: ../integration/test-wapc-component/build/test_component_s.wasm
  schematics:
    - name: logger
      providers:
        - wapc
      instances:
        logger:
          id: vino::v0::log
      connections:
        - <> => logger[input]
        - logger[output] => <>
