

{{~#each files}}
import { {{#import (replace . ".apex" "")}}Inputs as {{pascalCase namespace.name.value}}Inputs, Outputs as {{pascalCase namespace.name.value}}Outputs } {{/import}} from "{{replace . ".apex" ""}}"
{{/each}}

interface {
  {{#each files}}
  {{#import (replace . ".apex" "")}}
  {{snakeCase namespace.name.value }}(inputs: {{pascalCase namespace.name.value}}Inputs, outputs: {{pascalCase namespace.name.value}}Outputs) @rename("{{namespace.name.value}}")
  {{/import}}
  {{/each}}
}
